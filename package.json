{
  "devDependencies": {
    "next": "16.0.5",
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.11"
  },
  "scripts": {
    "dev": "concurrently \"npm run server --prefix server\" \"npm run dev --prefix client\"",
    "vercel-build": "npm run prepare && next build && npm install --prefix server && npm install --prefix client && npm run build --prefix client"
  }
}